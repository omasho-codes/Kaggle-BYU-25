# GPU-targeted requirements for BYU-25. Assumes a box with CUDA >= 12.1.
#
# For a CUDA build of PyTorch, install it with the matching index, e.g.:
#     pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
# then:
#     pip install -r requirements.txt
#
# Or use environment.yml (conda), which pins pytorch-cuda=12.1 directly.

# Core DL
torch>=2.1.0
torchvision>=0.16.0
ultralytics>=8.3.0

# Data & augmentation
numpy>=1.26.0
pandas>=2.1.0
opencv-python>=4.8.0
Pillow>=10.0.0
albumentations>=1.4.0

# Tomogram / volumetric IO
scikit-image>=0.22.0
scipy>=1.11.0

# Visualization / Grad-CAM
matplotlib>=3.8.0
seaborn>=0.13.0
grad-cam>=1.5.0

# Export & serving (GPU ORT; requires CUDA 12.x for 1.17+)
onnx>=1.15.0
onnxruntime-gpu>=1.17.0

# Dev tooling
tqdm>=4.66.0
pyyaml>=6.0.1
